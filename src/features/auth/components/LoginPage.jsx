import LoginForm from './LoginForm';

/**
 * LoginPage — routed entry point for the /login path.
 * AuthLayout handles the outer centering/card shell.
 */
function LoginPage() {
  return <LoginForm />;
}

export default LoginPage;
